#include <bits/stdc++.h>
#define sor3a ios_base::sync_with_stdio(false);cin.tie(NULL);

using namespace std;

bool isPerfectSquare(long long n) {
    return (double)(int)sqrt(n) == sqrt(n);
}

int main() {

    sor3a

    for (long long i=0; i<1e13-1; i++) {
        
    }


    return 0;
}